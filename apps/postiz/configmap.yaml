apiVersion: v1
kind: ConfigMap

metadata:
  name: postiz-config
  namespace: postiz

data:
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"

  DATABASE_HOST: "postgres-service"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "postiz"
